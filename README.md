# Multi-Camera-Person-Tracking-and-Re-Identification

chạy lệnh 
python learn_features.py --force --targets "targets"
python recognize_video.py --video "videos/init/3.mp4" (lệnh này chạy file nhận diện)
python custom_pipeline/visualize.py (lệnh này để xuất đặc trưng)
python custom_pipeline/train.py --epochs 80 (lệnh này để train)

